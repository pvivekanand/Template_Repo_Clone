<mvc:View controllerName="nw.epm.refapps.purchaseorders.approve.controller.App"
	displayBlock="true"
	busy="{= ${globalProperties>/isMetaDataLoading} || ${globalProperties>/isBusyApproving} }"
	busyIndicatorDelay="{= ${globalProperties>/metaDataLoaded} ? ${globalProperties>/originalBusyDelay} : 0 }" 
	xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc">
	<SplitApp id="approvalApp" />
</mvc:View>